---

radarr_rclone_qbittorrent_is_enabled: '{{ (radarr_rclone_qbittorrent_type is defined) and
                                          (radarr_rclone_qbittorrent_type is not none) and
                                          (radarr_rclone_qbittorrent_type | trim | length > 0) and
                                          (radarr_rclone_qbittorrent_name is defined) and
                                          (radarr_rclone_qbittorrent_name is not none) and
                                          (radarr_rclone_qbittorrent_name | trim | length > 0) and
                                          (radarr_rclone_qbittorrent_mount_path is defined) and
                                          (radarr_rclone_qbittorrent_mount_path is not none) and
                                          (radarr_rclone_qbittorrent_mount_path | trim | length > 0) and
                                          (radarr_rclone_qbittorrent_host is defined) and
                                          (radarr_rclone_qbittorrent_host is not none) and
                                          (radarr_rclone_qbittorrent_host | trim | length > 0) and
                                          (radarr_rclone_qbittorrent_user is defined) and
                                          (radarr_rclone_qbittorrent_user is not none) and
                                          (radarr_rclone_qbittorrent_user | trim | length > 0) and
                                          (radarr_rclone_qbittorrent_pass is defined) and
                                          (radarr_rclone_qbittorrent_pass is not none) and
                                          (radarr_rclone_qbittorrent_pass | trim | length > 0) and
                                          (radarr_rclone_qbittorrent_key_file_pass is defined) and
                                          (radarr_rclone_qbittorrent_key_file_pass is not none) and
                                          (radarr_rclone_qbittorrent_key_file_pass | trim | length > 0) }}'
